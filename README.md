In the old folder, the code is written in an easy-to-read way, with too many nested loops
The faster version is faster than the old one, as the nested loops have been removed 
The SAT-UNSAT notebook checks for satisfiability within the range, while the optimize solves with the optimize/minimize function of z3
